apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: network-tools-stag
  namespace: datalake-he
spec:
  project: datalake
  source:
    repoURL: http://source.cyberspace.vn/nhatcm1/he_k8s.git
    targetRevision: main
    path: deploy/k8s
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: network-tools-stag
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 3
